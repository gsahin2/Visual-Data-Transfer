# Samples

Place golden captures, reference PNG frames, and short screen recordings here.

The `python/generate_test_frames.py` script writes examples to `samples/generated/` (git-ignored by default—create it with the generator).

Suggested assets for future CI:

- A short H.264 clip with a known payload.
- Still PNG exports for each chunk of multi-frame sessions.
